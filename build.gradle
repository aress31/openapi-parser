apply plugin: 'java'

dependencies {
    compile files("lib/com/google/gson/gson-2.8.0.jar")
}

sourceSets {
    main {
        java {
            srcDir 'burp'
        }
    }
}

task fatJar(type: Jar) {
    baseName = project.name + '-all'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}
